@model Secretaria.Model.TbCurso

@{
    ViewData["Title"] = "Deletar";
}

<h1>Deletar</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NomeCurso)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomeCurso)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Descricao)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Descricao)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="IdCurso" />
        <input type="submit" value="Deletar" class="btn btn-light btn-sm" /> |
        <a asp-action="Index" class="btn btn-danger btn-sm">Voltar</a>
    </form>
</div>
